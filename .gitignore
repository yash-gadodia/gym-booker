# Secrets — NEVER commit
.env
.env.*
!.env.example
auth.json
# Multi-user creds + per-user storage state
users.json
users-auth/
# Per-date overrides + pause windows (managed by ~/.openclaw/scripts/gym-override.py)
overrides.json

# Per-run artifacts
runs/
screenshots/
netlog.json
check-status.png
cancel-modal.png
cancel-after.png
my-schedule.png
# Recon captures contain Bearer JWTs — never commit
recon-api/

# Dependencies
node_modules/

# Logs
*.log

# OS
.DS_Store
