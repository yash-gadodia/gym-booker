# Secrets — NEVER commit
.env
.env.*
!.env.example
auth.json

# Per-run artifacts
runs/
screenshots/
netlog.json
check-status.png
cancel-modal.png
cancel-after.png
my-schedule.png
# Recon captures contain Bearer JWTs — never commit
recon-api/

# Dependencies
node_modules/

# Logs
*.log

# OS
.DS_Store
